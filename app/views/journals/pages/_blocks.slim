.mh-blocks-wrapper
  - @page.blocks.each_with_index do |block, index|
    .one-block class="#{index % 2 == 1 ? 'block-right' : 'block-left'}"
      .block-inner
        - if block.image?
          span.img-block
            = image_tag block.image_url, class: 'img'
        span.text-block
          = temple_format block.content.bbcode_to_html
      .clear
