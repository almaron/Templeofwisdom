Игрок просит вашего внимания в теме [url=/temple/<%= topic.forum_id %>/t/<%= topic.id %>?post=<%= post.id %>]<%= topic.head %>[/url].
<% if comment.present? %>
    Комментарий: <%= comment %>
<% end %>