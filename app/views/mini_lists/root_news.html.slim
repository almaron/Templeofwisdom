.newsblock
  h2 Новости монастыря
  - @news.each do |news|
    .news-single
      .news_block
        .news-single-title
          span.news-single-date = I18n.l(news.created_at)
          span.news-single-date-title-text = news.head
        .news-single-text
          .text
            = temple_format(news.text.bbcode_to_html)
          .author
            | / #{news.author}
